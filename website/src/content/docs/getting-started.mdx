---
title: Getting Started
description: Install Alchemy and create your first Stack in under two minutes.
sidebar:
  order: 1
---

import Terminal from "../../components/Terminal.astro";
import { Code, Tabs, TabItem } from "@astrojs/starlight/components";
import { alchemyVersion, effectVersion } from "../../versions";

export const bunPkgs = `alchemy@${alchemyVersion} effect@${effectVersion} @effect/platform-bun@${effectVersion}`;
export const nodePkgs = `alchemy@${alchemyVersion} effect@${effectVersion} @effect/platform-node@${effectVersion}`;

## Prerequisites

- [Bun](https://bun.sh) (recommended) or Node.js 20+
- A [Cloudflare](https://dash.cloudflare.com/sign-up) account with an API token

## Create a project

<Tabs syncKey="pkgManager">
  <TabItem label="bun" icon="bun">
    <Code code="mkdir my-app && cd my-app && bun init -y" lang="sh" />
  </TabItem>
  <TabItem label="npm" icon="npm">
    <Code code="mkdir my-app && cd my-app && npm init -y" lang="sh" />
  </TabItem>
  <TabItem label="pnpm" icon="pnpm">
    <Code code="mkdir my-app && cd my-app && pnpm init" lang="sh" />
  </TabItem>
  <TabItem label="yarn" icon="seti:yarn">
    <Code code="mkdir my-app && cd my-app && yarn init -y" lang="sh" />
  </TabItem>
</Tabs>

## Install

<Tabs syncKey="pkgManager">
  <TabItem label="bun" icon="bun">
    <Code code={`bun add ${bunPkgs}`} lang="sh" />
  </TabItem>
  <TabItem label="npm" icon="npm">
    <Code code={`npm install ${nodePkgs}`} lang="sh" />
  </TabItem>
  <TabItem label="pnpm" icon="pnpm">
    <Code code={`pnpm add ${nodePkgs}`} lang="sh" />
  </TabItem>
  <TabItem label="yarn" icon="seti:yarn">
    <Code code={`yarn add ${nodePkgs}`} lang="sh" />
  </TabItem>
</Tabs>

## Create your Stack

Every Alchemy program starts with a Stack — create `alchemy.run.ts`:

```typescript
// alchemy.run.ts
import * as Alchemy from "alchemy";
import * as Cloudflare from "alchemy/Cloudflare";
import * as Effect from "effect/Effect";

export default Alchemy.Stack(
  "MyApp",
  {
    providers: Cloudflare.providers(),
    state: Cloudflare.state(),
  },
  Effect.gen(function* () {
    const bucket = yield* Cloudflare.R2Bucket("Bucket");

    return {
      bucketName: bucket.bucketName,
    };
  }),
);
```

## Deploy

```sh
alchemy deploy
```

<Terminal content={`[u]Plan[/u]: [g]1 to create[/g]
[g]+[/g] [b]Bucket[/b] [d](Cloudflare.R2Bucket)[/d]

Proceed?
◉ Yes ○ No
[g]✓[/g] [b]Bucket[/b] [d](Cloudflare.R2Bucket)[/d] [g]created[/g]
{
[s2]bucketName: "myapp-bucket-a1b2c3d4e5",
}`} />

That's it — you have a live R2 Bucket on Cloudflare.

## What's next

<div class="next-steps">

<a href="/tutorial/part-1" class="next-card">
  <strong>Tutorial</strong>
  <span>
    New to Alchemy? Follow the hands-on tutorial from zero to CI/CD in five
    parts.
  </span>
</a>

<a href="/guides/migrating-from-v1" class="next-card">
  <strong>Migrating from v1</strong>
  <span>
    Already using Alchemy v1 with async/await? Learn what changed and how to
    upgrade.
  </span>
</a>

</div>
