/**
 * Re-export the shared traits system from sdk-core.
 *
 * PlanetScale uses the shared HTTP traits directly.
 * Import from this module for SDK-consistent imports:
 *
 * @example
 * ```ts
 * import * as T from "@distilled.cloud/planetscale/traits";
 * ```
 */
export * from "@distilled.cloud/core/traits";
