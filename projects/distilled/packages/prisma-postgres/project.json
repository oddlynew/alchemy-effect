{
  "name": "@oddlynew/distilled-prisma-postgres",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsgo -b --checkers 1 --builders 1",
        "cwd": "{projectRoot}"
      },
      "outputs": [
        "{projectRoot}/lib",
        "{projectRoot}/.tsbuild",
        "{projectRoot}/tsconfig.tsbuildinfo"
      ],
      "cache": true,
      "dependsOn": ["^build"]
    }
  }
}
