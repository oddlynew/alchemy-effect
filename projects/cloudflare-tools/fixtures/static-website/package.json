{
  "name": "@oddlynew/cloudflare-tools-fixture-static-website",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "bun test build-manifest.test.ts && playwright test",
    "test:update-snapshots": "playwright test --update-snapshots",
    "pretest": "playwright install chromium"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.36.4",
    "@oddlynew/distilled-cloudflare-vite-plugin": "workspace:*",
    "@oddlynew/distilled-test-utils": "workspace:*",
    "@playwright/test": "catalog:",
    "vite": "catalog:"
  }
}
